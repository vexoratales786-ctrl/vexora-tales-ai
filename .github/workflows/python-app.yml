name: Vexora Tales AI (legacy disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

# Legacy automation is intentionally disabled.
# Sameena AI v2 must be reviewed and deployed before any publishing automation is restored.
# This prevents accidental public YouTube uploads from the old main.py flow.

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Safety lock
        run: echo "Legacy auto-upload workflow is disabled. Sameena AI v2 requires explicit upload approval."
